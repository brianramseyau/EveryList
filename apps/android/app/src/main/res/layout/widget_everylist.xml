<?xml version="1.0" encoding="utf-8"?>
<!-- Widget root layout (RemoteViews). Header: list selector top-left, then show/hide-done,
     refresh, and quick-add (+) top-right — mirroring Google Tasks. Below: an optional offline
     note, an empty state, and the scrollable item ListView. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="10dp"
    android:background="@drawable/widget_background">

    <LinearLayout
        android:id="@+id/widget_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/widget_list_button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:padding="4dp"
            android:singleLine="true"
            android:ellipsize="end"
            android:text="@string/widget_default_title"
            android:textColor="@color/widget_ink"
            android:textSize="15sp"
            android:textStyle="bold"
            android:drawablePadding="4dp"
            android:background="?android:attr/selectableItemBackground" />

        <TextView
            android:id="@+id/widget_toggle_completed"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="6dp"
            android:text="@string/widget_show_done"
            android:textColor="@color/widget_accent"
            android:textSize="12sp"
            android:background="?android:attr/selectableItemBackground" />

        <ImageButton
            android:id="@+id/widget_refresh"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_widget_refresh"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/widget_refresh" />

        <ImageButton
            android:id="@+id/widget_add"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:src="@drawable/ic_widget_add"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/widget_add" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_error"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="4dp"
        android:text="@string/widget_offline_note"
        android:textColor="@color/widget_warn"
        android:textSize="12sp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="@string/widget_empty"
        android:textColor="@color/widget_muted"
        android:textSize="13sp"
        android:visibility="gone" />

    <ListView
        android:id="@+id/widget_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="2dp"
        android:divider="@null"
        android:dividerHeight="0dp" />
</LinearLayout>