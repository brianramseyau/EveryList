<?xml version="1.0" encoding="utf-8"?>
<!-- The quick-add text field's own container — same treatment as widget_config_section_background
     (12dp radius, bordered, transparent fill) so the popup matches the config screen's card style. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@android:color/transparent" />
    <corners android:radius="12dp" />
    <stroke android:width="1dp" android:color="@color/widget_border" />
</shape>
