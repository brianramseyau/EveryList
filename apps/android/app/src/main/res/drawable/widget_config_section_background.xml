<?xml version="1.0" encoding="utf-8"?>
<!-- The "pick a list" section's own container — a step tighter radius than the popup card around
     it (12dp vs 20dp), matching the app's rounded-lg settings-row sections on the web. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@android:color/transparent" />
    <corners android:radius="12dp" />
    <stroke android:width="1dp" android:color="@color/widget_border" />
</shape>
